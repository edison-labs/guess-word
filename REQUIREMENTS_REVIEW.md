# GuessWord MVP 需求评审

评审结论：**有条件通过，进入工程开发；工程 MVP 和小范围用户内测采用不同准出门槛。**

执行优先级为 `AI_BUILD_PROMPT.md` 明确 MVP 限定 > `PRODUCT_DESIGN.md` > 本评审假设。

## 已冻结口径

| 议题 | 本轮裁决 |
|---|---|
| 题库规模 | 实际实现 102 题、6 类且均衡分布；逐题真人试玩与真实模型标定仍是内测质量门槛。 |
| 输入 | NFKC、去首尾空白后只接受 1～10 个连续汉字；不支持字母数字，不自动简繁转换。 |
| 模式 | 无需登录；玩家先选六个分类之一，再在该分类中随机抽题。每日一词延期。 |
| 分享与设置 | 均不进入 P0；仅尊重 `prefers-reduced-motion`。 |
| 恢复 | 浏览器只保存不可推导答案的 `gameId` 和恢复令牌，完整状态由服务端 D1 保存。 |
| 猜测次数 | 最终命中词计入；无效、重复和评分失败均不计入。 |
| 最佳猜测 | 定义为命中前最高分的非答案词；同分取更早提交。 |
| 本地战绩 | 总局数只统计已猜中或已放弃的结算局；最佳次数只统计胜局；同一局幂等记录。 |
| 历史排序 | 默认分数降序、同分按序号；时间排序按提交序号升序。 |
| 真实模型 | 无凭据可用显式测试适配器完成工程验收，但不可据此宣称真实语义体验通过。 |
| 生产保护 | 生产缺少真实模型配置或选择测试评分器时必须失败，不能回退。 |

## 明确非目标

- 登录、账号、昵称、排行榜和跨设备同步；
- 每日一词、连续天数和分享卡；
- 支付、广告、多人、好友房和管理后台；
- 100～500 题内容运营和真实模型人工标定；
- 公开生产部署。

## 准出层级

- **工程 MVP：** P0 功能、102 题、确定性测试路径、单元/API/E2E、构建、安全泄露检查全部通过后可准出。
- **小范围用户内测：** 在工程 MVP 之上，还必须完成有效真实模型的逐题中文语义回归与真人试玩。当前不满足这一层级。
