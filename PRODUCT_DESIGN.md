# GuessWord 产品设计文档

> 版本：V1.0
> 阶段：MVP 方案
> 产品形态：响应式 Web 游戏
> 默认语言：简体中文

## 1. 产品概述

GuessWord 是一款基于语义关联度的猜词游戏。每局游戏隐藏一个目标词，只向玩家展示目标词所属的大致范围。玩家可以不断输入猜测词，系统以百分比反馈该词与目标词的语义关联程度。玩家根据“越来越接近”的反馈调整思路，最终猜中答案。

一句话定位：**根据语义关联强弱，寻找隐藏词。**

### 1.1 核心体验

- 输入没有固定选项，允许玩家自由联想。
- 每次猜测都会获得明确、连续的关联度反馈。
- 猜测历史形成可复盘的思考路径。
- 单局时间短，但通过答案库和每日题保持复玩性。

### 1.2 产品目标

- 让新玩家在 10 秒内理解规则并开始第一次猜测。
- 让一局游戏通常在 3～10 分钟内结束。
- 让关联度反馈基本符合人的语义直觉。
- 建立适合分享和朋友讨论的轻量文字游戏体验。

### 1.3 非目标

MVP 阶段暂不包含：

- 实时多人对战；
- 玩家自定义题库；
- 复杂账号与社交关系；
- 现金奖励或付费道具；
- 完全依赖大模型临时生成答案与分数。

## 2. 目标用户与场景

### 2.1 目标用户

- 喜欢文字、推理、解谜游戏的用户；
- 希望利用碎片时间进行轻度脑力活动的用户；
- 喜欢把游戏结果分享到群聊或社交平台的用户；
- 对 Wordle、Contexto、成语类游戏有兴趣的用户。

### 2.2 使用场景

- 通勤、排队、午休时独自游玩；
- 朋友聚会时共同讨论答案；
- 每天完成一题并分享成绩；
- 主播或内容创作者进行互动挑战。

## 3. 游戏规则

### 3.1 开局

系统从经过审核的答案库中选择一个目标词，并展示一个范围提示，例如：

- 范围：动物
- 范围：食物
- 范围：职业
- 范围：自然现象
- 范围：抽象概念

范围提示用于帮助玩家确定起点，但不能直接暴露答案。

### 3.2 猜测

玩家在输入框中输入一个词并提交。系统完成格式校验和语义计算后，返回：

- 猜测词；
- 关联度百分比；
- 关联等级；
- 本局猜测序号；
- 当前最佳猜测。

同一个词重复输入时不重复计入有效猜测，并提示“这个词已经猜过了”。

### 3.3 关联度

关联度显示范围为 0%～100%，数值越高表示越接近目标词。

| 关联度 | 状态 | 建议视觉反馈 |
|---|---|---|
| 0%～19.9% | 几乎无关 | 蓝灰色 |
| 20%～39.9% | 关联较弱 | 蓝色 |
| 40%～59.9% | 有些关联 | 黄色 |
| 60%～79.9% | 关联较强 | 橙色 |
| 80%～94.9% | 高度相关 | 红色 |
| 95.000%～99.900% | 非常接近 | 亮红色、轻微动效 |
| 100% | 猜中 | 成功动效 |

只有标准化后的输入与目标词完全一致时才显示 100.000% 并判定胜利。其他词即使高度相似，也最多显示 99.900%。

### 3.4 结束条件

- 猜中：玩家输入目标词，立即结束本局；
- 查看答案：玩家二次确认后结束本局并揭晓答案；
- 游戏不限制普通猜测次数和时间。

### 3.5 提示机制

每局提供最多三次渐进式提示：

1. 第一次提示：目标词字数；
2. 第二次提示：更具体的子类别或使用场景；
3. 第三次提示：提供一个高关联度参考词，但不展示答案。

每使用一次提示，结算页记录一次提示使用，不再强调最少次数成绩。

## 4. 游戏模式

### 4.1 分类随机模式（MVP）

- 开局先从动物、食物、职业、自然现象、抽象概念、日常物品中选择分类；
- 每局只在所选分类中随机抽取目标词；
- 本地记录最近战绩；
- 适合持续游玩和产品测试。

### 4.2 每日一词（V1.1）

- 所有玩家每天面对同一个目标词；
- 每天零点按产品默认时区更新；
- 记录连续完成天数；
- 生成不泄露答案的分享卡片。

## 5. 信息架构

MVP 采用单页面结构，减少跳转。

```text
首页 / 游戏页
├── 分类选择区（未开局时）
│   └── 六个题目分类
├── 顶部栏
│   ├── Logo / GuessWord
│   ├── 游戏规则
│   └── 设置
├── 题目区
│   ├── 范围提示
│   ├── 当前最佳关联度
│   └── 已猜次数
├── 输入区
│   ├── 猜测词输入框
│   └── 提交按钮
├── 猜测记录区
│   ├── 按提交时间查看
│   └── 按关联度排序
├── 辅助操作
│   ├── 获取提示
│   └── 查看答案
└── 结算弹窗
    ├── 正确答案
    ├── 猜测次数 / 用时 / 提示次数
    ├── 最接近的猜测
    ├── 再玩一局
    └── 分享结果
```

## 6. 页面与交互设计

### 6.1 首屏

首次访问先展示紧凑的六分类选择区，不使用占据整屏的宣传内容。选定分类后直接展示范围提示和输入框，并用一句话解释规则：

> 输入任意词语，根据关联度找到隐藏答案。

输入框默认自动聚焦；移动端弹出键盘时，提交按钮仍应可见。

### 6.2 输入交互

- 点击按钮或按 Enter 提交；
- 提交期间显示短暂计算状态，并防止重复提交；
- 成功提交后清空输入框并保持焦点；
- 自动滚动到最新记录；
- 空输入、过长输入和无效字符在输入框附近提示。

建议输入限制：1～10 个汉字，允许常见中文词语和少量字母数字组合；首尾空格自动移除。

### 6.3 猜测记录

默认按关联度从高到低排列，让玩家快速找到最有效的思路；同时提供“按时间”切换。

每条记录至少展示：

- 排名或序号；
- 猜测词；
- 关联度数值；
- 对应颜色进度条。

新提交的词需要短暂高亮。刷新页面后，本局记录仍然保留。

### 6.4 结算

猜中后展示：

- “猜中了！”成功反馈；
- 正确答案；
- 有效猜测次数；
- 完成用时；
- 使用提示次数；
- 猜测路径中的最高关联词；
- “再玩一局”和“分享结果”按钮。

分享内容不能直接包含答案，建议使用色块或冷热符号表达猜测过程。

## 7. 功能需求

### 7.1 P0：MVP 必须实现

| 编号 | 功能 | 说明 |
|---|---|---|
| FR-01 | 开始游戏 | 玩家选择分类后，从该分类创建一局随机题目 |
| FR-02 | 提交猜测 | 校验输入并请求关联度 |
| FR-03 | 关联度反馈 | 显示百分比、关联等级和进度条 |
| FR-04 | 猜测历史 | 展示、排序并持久化本局记录 |
| FR-05 | 胜利判定 | 精确命中目标词后进入结算 |
| FR-06 | 渐进提示 | 每局最多使用三次提示 |
| FR-07 | 查看答案 | 二次确认后揭晓答案并结束本局 |
| FR-08 | 再来一局 | 清理当前状态并返回分类选择 |
| FR-09 | 本地战绩 | 保存总局数、胜局数和最佳成绩 |
| FR-10 | 响应式适配 | 支持手机与桌面浏览器 |

### 7.2 P1：正式版本增强

- 每日一词；
- 连续完成天数；
- 分享卡片；
- 深色模式；
- 音效与动效开关；
- 排行榜和匿名昵称；
- 多语言题库。

### 7.3 P2：后续探索

- 好友房间和实时竞速；
- 玩家出题；
- 主题题包；
- AI 生成复盘和联想路径；
- 创作者直播互动模式。

## 8. 语义评分设计

### 8.1 基本原则

- 分数必须稳定：同一题目和输入始终得到相同结果；
- 分数必须可比较：关联度更高通常应代表语义更接近；
- 分数必须可解释：近义词、上下位词、同场景词应获得合理反馈；
- 答案不能在浏览器端直接暴露。

### 8.2 推荐方案

服务端使用中文语义模型评估猜测词与目标词的关系。不要用随机尾数模拟精度，应把模型原始判断稳定校准为更符合玩家直觉的 0.000%～99.900%。

推荐流程：

```text
输入标准化
→ 有效性与敏感内容检查
→ 获取或计算猜测词向量
→ 与目标词向量计算相似度
→ 根据标定样本映射为关联度
→ 精确命中检查
→ 返回结果
```

答案词向量应预先计算并缓存。常见猜测词的向量和结果也应缓存，以降低响应时间和计算成本。

### 8.3 分数校准

上线前准备一批人工标注词对，包括：

- 同义词；
- 上位词与下位词；
- 同类别词；
- 常见场景关联词；
- 弱关联词；
- 无关联词。

使用这些样本确定映射曲线，并对多义词、专有名词和模型异常高分进行人工复核。产品界面统一使用“关联度”，不声称它是严格的语言学相似概率。

## 9. 题库设计

### 9.1 MVP 规模

- 首批准备 300～500 个目标词；
- 以 2～4 字常用名词为主；
- 每个范围至少包含 20 个候选答案；
- 每个答案配置范围、子类别、字数提示和高关联提示词。

### 9.2 选词标准

目标词应满足：

- 大部分目标用户认识；
- 含义相对明确；
- 能产生足够多的联想路径；
- 不依赖特定地区小众知识；
- 不包含敏感、歧视、成人或高风险内容。

优先使用实体名词。多义程度高、语义模型表现不稳定、正确写法存在争议的词暂不进入答案库。

### 9.3 题目配置示例

```json
{
  "id": "animal_001",
  "answer": "企鹅",
  "category": "动物",
  "subcategory": "生活在寒冷地区的鸟类",
  "length": 2,
  "hotHint": "南极",
  "status": "active"
}
```

实际接口不能把 `answer` 或可逆推出答案的信息发送到前端。

## 10. 数据与状态

### 10.1 单局数据

- 游戏局 ID；
- 题目 ID；
- 开始与结束时间；
- 游戏状态；
- 猜测记录；
- 提示使用次数；
- 是否胜利；
- 有效猜测总数。

### 10.2 本地数据

MVP 可在浏览器本地保存：

- 当前未结束的一局；
- 最近 20 局战绩；
- 音效、动效和主题偏好；
- 匿名设备统计标识。

目标答案和服务端密钥不得保存到浏览器本地。

### 10.3 核心事件

- `game_started`
- `guess_submitted`
- `hint_used`
- `game_won`
- `game_abandoned`
- `result_shared`
- `new_game_started`

事件数据不记录用户输入的敏感自由文本；用于模型改进的猜测词应经过隐私评估和脱敏策略。

## 11. 非功能需求

### 11.1 性能

- 首屏在常见移动网络下尽快可交互；
- 95% 的关联度请求在 1 秒内返回；
- 超过 3 秒时显示明确的等待状态；
- 请求失败时允许保留输入并重试。

### 11.2 可用性与无障碍

- 键盘可以完成主要操作；
- 颜色之外同时显示数值和文字状态；
- 文本与背景保持足够对比度；
- 尊重系统“减少动态效果”设置；
- 关键按钮具有可读标签和清晰焦点状态。

### 11.3 安全

- 限制单设备和单 IP 的请求频率；
- 服务端校验所有输入；
- 不在前端返回完整答案库；
- 对提示、放弃和结算接口进行游戏状态校验；
- 日志避免记录不必要的个人信息。

## 12. 成功指标

MVP 上线后重点观察：

| 指标 | 目标方向 |
|---|---|
| 首次猜测提交率 | 越高越好，用于判断规则是否易懂 |
| 单局完成率 | 判断题目难度与反馈质量 |
| 猜中率 | 过低说明题目或评分不可用，过高说明挑战不足 |
| 猜中中位次数 | 用于校准题目难度 |
| 提示使用率 | 判断提示是否必要、题目是否过难 |
| 再来一局率 | 衡量核心玩法吸引力 |
| 次日回访率 | 衡量长期复玩性 |
| 评分异常反馈率 | 衡量语义模型质量 |

建议首轮内测目标：多数普通难度题可在 10～40 次有效猜测内完成。

## 13. MVP 验收标准

满足以下条件即可进入小范围内测：

1. 用户打开页面后无需登录即可开始游戏；
2. 页面明确展示范围提示、输入框和规则说明；
3. 输入有效词语后能稳定返回 0.000%～99.900% 的三位小数关联度；
4. 精确输入目标词时返回 100% 并结束本局；
5. 重复或无效输入不会增加有效猜测次数；
6. 猜测历史可按关联度和时间查看；
7. 刷新页面后可以恢复未结束的游戏；
8. 三档提示按顺序解锁且不会直接泄露答案；
9. 查看答案前有二次确认，确认后正确结束本局并显示结果；
10. 手机和桌面端均可顺畅完成整局；
11. 不查看页面源代码或网络响应就能直接获得目标答案；
12. 至少 100 道经过人工试玩的题目达到可玩标准。

## 14. 迭代计划

### 阶段一：可玩原型

- 完成单页面游戏流程；
- 接入基础语义评分；
- 准备 30 道测试题；
- 验证分数是否符合直觉。

### 阶段二：MVP 内测

- 扩充至 100～300 道题；
- 完成提示、放弃、恢复和本地战绩；
- 增加数据统计与异常反馈；
- 针对高频问题调整评分曲线。

### 阶段三：公开版本

- 上线每日一词、连续完成和分享；
- 完善题库运营后台；
- 优化性能、安全和内容审核；
- 根据数据决定是否开发账号与多人模式。

## 15. 主要风险与应对

| 风险 | 影响 | 应对方案 |
|---|---|---|
| 关联度不符合直觉 | 玩家无法推理、快速流失 | 人工标注校准、异常反馈、定期回归测试 |
| 多义词导致分数混乱 | 相同输入出现误导 | 优先选低歧义答案，增加词义与语境处理 |
| 前端泄露答案 | 失去游戏公平性 | 答案只保存在服务端，接口按局返回结果 |
| 题目过难或过简单 | 完成率失衡 | 题目分级，依据完成数据调整范围和提示 |
| 模型调用成本过高 | 难以扩大用户规模 | 预计算、缓存、批处理和请求限流 |
| 用户输入不当内容 | 带来内容与合规风险 | 输入校验、敏感内容策略和日志最小化 |

## 16. 待确认事项

进入开发前需要最终确认：

- 首发仅支持中文，还是同时支持英文；
- 首发是否需要“每日一词”，还是只做分类随机模式；
- 关联度由本地部署模型还是第三方 API 提供；
- 是否允许短语、专有名词和英文输入；
- 是否需要账号、排行榜和跨设备同步；
- 产品正式中文名是否继续使用 GuessWord，或另设中文品牌名。
