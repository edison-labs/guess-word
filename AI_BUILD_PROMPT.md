# GuessWord Web MVP — AI 开发、测试与验收 Prompt

你是一名资深全栈工程师、产品工程师和测试负责人。请在当前代码仓库内独立完成 GuessWord Web MVP 的开发、测试与验收。不要只生成方案、演示页面或代码片段；持续工作到产品可以本地完整运行，并给出基于证据的验收报告。

## 一、任务目标

开发一个手机优先、同时适配桌面的中文语义猜词 Web 游戏。

每局隐藏一个目标词，并向玩家展示目标词所属的大致范围。玩家可以不断输入词语，系统返回该词与目标词的语义关联度百分比。玩家根据关联度逐步接近并最终猜中答案。

项目名称：`GuessWord`
仓库名称：`guess-word`
产品定位：根据语义关联强弱寻找隐藏词。

完整产品需求位于：

```text
PRODUCT_DESIGN.md
```

开始开发前必须完整阅读该文件，并以它作为产品需求的主要依据。如果本文与产品文档存在冲突，以本文明确限定的 MVP 范围为准；仍有歧义时，优先选择实现简单、体验完整、后续易扩展的方案，并在最终报告中说明假设。

## 二、工作原则

1. 先检查仓库现状、已有代码、配置和用户未提交的修改，不要覆盖无关内容。
2. 如果项目尚未初始化，选择成熟、维护活跃、适合响应式 Web 和服务端 API 的 TypeScript 技术栈进行初始化；说明选择原因，但不要停下来等待技术选型确认。
3. 先完成一条可玩的端到端主流程，再补充提示、恢复、测试和细节。
4. 语义关联度必须由服务端计算，不能在浏览器代码、接口响应、页面状态或本地存储中泄露答案。
5. 不得用随机数、字符串编辑距离或硬编码单一答案的方式冒充语义评分。
6. 不得在代码中写入密钥。所有外部服务配置都必须通过环境变量提供，并维护 `.env.example`。
7. 如果真实语义模型需要用户尚未提供的密钥或外部服务：
   - 仍需完成完整架构、适配器、界面、错误处理和自动化测试；
   - 提供一个明确标记为开发/测试用途的确定性测试适配器；
   - 测试适配器只能在开发或测试环境启用，生产构建不得静默回退到假数据；
   - 在最终报告中清楚列出启用真实模型所需的唯一操作；
   - 不要假装已经验证真实模型效果。
8. 不要为了“看起来完整”加入登录、排行榜、支付、广告、多人对战或管理后台。
9. 不执行付费操作，不发布到公开生产环境，除非用户另有明确授权。
10. 遇到普通工程问题时自行诊断并修复；只有缺少外部账号、密钥或必须由用户作出的高风险决定时才请求用户处理。

## 三、MVP 功能范围

### 1. 开始与恢复游戏

- 用户无需登录即可开始随机模式。
- 新游戏由服务端创建并返回不可推导答案的游戏局标识、范围提示和开始时间。
- 刷新页面后可以恢复同一局及全部有效猜测记录。
- 已结束的游戏不能继续提交猜测。
- “再来一局”创建新题并清理当前页面状态。

### 2. 范围提示

- 每局展示一个宽泛范围，例如动物、食物、职业、自然现象或抽象概念。
- 前端不得获得完整题目对象、答案、答案词向量或能直接映射出答案的内部标识。

### 3. 提交猜测

- 接受 1～10 个字符的中文词语；去除首尾空格并统一常见字符形式。
- Enter 键和提交按钮都能提交。
- 提交期间禁止重复请求，并显示短暂计算状态。
- 提交成功后清空输入框并保持输入焦点。
- 空输入、超长输入、重复输入、无效字符、请求失败都要给出就近且清晰的提示。
- 同一个标准化词重复输入时不调用评分服务，也不增加有效猜测次数。

### 4. 关联度反馈

- 返回并展示 0%～100% 的关联度、关联等级和可视化进度条。
- 非答案词最高只能显示 99.900%。
- 只有标准化输入与目标词精确一致时显示 100%，并判定胜利。
- 颜色不能是唯一反馈方式，必须同时提供数字和文字状态。
- 建议使用以下区间：
  - 0%～19.9%：几乎无关
  - 20%～39.9%：关联较弱
  - 40%～59.9%：有些关联
  - 60%～79.9%：关联较强
  - 80%～94.9%：高度相关
  - 95.000%～99.900%：非常接近
  - 100%：猜中

### 5. 猜测历史

- 每条记录显示猜测词、关联度、关联等级和猜测序号。
- 默认按关联度从高到低显示。
- 可切换为按提交时间显示。
- 新提交的记录有克制的高亮反馈。
- 清楚展示当前最佳猜测和有效猜测总数。

### 6. 渐进式提示

每局最多三次，必须按顺序使用：

1. 目标词字数；
2. 更具体的子类别或使用场景；
3. 一个经过配置或安全计算的高关联参考词。

提示接口只能返回当前层级需要的信息，不能一次返回后续提示或答案。刷新后提示状态必须正确恢复。

### 7. 查看答案与结算

- “查看答案”前必须二次确认。
- 确认后揭晓答案并结束本局。
- 猜中或查看答案后显示结算视图。
- 猜中结算至少展示答案、有效猜测次数、完成用时、提示次数和最佳猜测。
- 提供“再来一局”。
- 分享功能不作为 P0 必做；如果实现，分享内容不得泄露答案。

### 8. 本地战绩

使用浏览器本地存储保存：

- 总局数；
- 猜中局数；
- 最佳有效猜测次数；
- 最近最多 20 局摘要。

目标答案、密钥和答案向量不得存入浏览器本地。

## 四、题库与语义评分

### 1. 题库

- 至少提供 30 道结构完整的开发题目，覆盖不少于 5 个范围。
- 每题至少包含：内部 ID、答案、范围、子类别、字数、高关联提示词和启用状态。
- 目标词以常见、低歧义、2～4 字中文名词为主。
- 增加自动校验，保证 ID 唯一、必填字段完整、答案长度正确、提示词不等于答案。
- 题库只能保存在服务端可访问的位置。

### 2. 评分架构

实现清晰的语义评分适配器接口，至少负责：

- 输入词向量化；
- 目标词向量获取或预计算；
- 余弦相似度计算；
- 原始相似度到 0.000%～99.900% 的稳定三位小数校准；
- 超时、限流和供应商错误处理；
- 常见词结果缓存。

优先选择真正支持中文语义的嵌入模型。选择具体模型和供应商前，核对其当前官方文档、语言能力、价格、速率限制和服务端运行条件，并在 README 中记录选择依据。

### 3. 测试评分适配器

为自动化测试提供确定性适配器，确保测试不依赖网络和付费 API。它必须：

- 对相同输入始终返回相同结果；
- 支持至少一组从低到高的已知词对；
- 支持精确答案 100% 的独立判定；
- 不能在生产环境配置缺失时被自动启用。

## 五、界面与体验要求

- 采用简洁、专注、具有轻度游戏感的视觉风格。
- 首屏直接看到产品名、范围提示、输入框、提交按钮和简短规则，不要做大面积宣传 Banner。
- 优先适配 375px 左右的手机宽度，同时保证 1440px 桌面宽度不过度拉伸。
- 输入框和主要按钮必须适合触摸操作。
- 加载、空状态、错误状态、无网络状态、胜利状态和放弃状态都必须完整。
- 支持键盘操作，并提供清晰焦点样式和可访问名称。
- 尊重 `prefers-reduced-motion`。
- 不使用颜色作为唯一信息表达方式。
- 避免不必要的动画、复杂导航和通用后台式布局。

## 六、接口与安全要求

至少需要覆盖以下服务端能力，具体路由命名可由你合理设计：

- 创建游戏；
- 获取或恢复游戏状态；
- 提交猜测；
- 获取下一条提示；
- 放弃游戏。

必须满足：

- 所有输入均在服务端再次校验；
- 游戏局标识不可顺序枚举；
- 不信任前端提交的题目、分数、猜测次数和游戏状态；
- 对提交猜测接口实施合理的频率限制或提供可插拔实现；
- 避免通过错误消息、序列化对象、日志或 source map 泄露答案；
- 客户端接口类型与服务端返回保持一致；
- API 错误采用一致、可识别且不暴露内部实现的格式。

## 七、必须完成的测试

### 1. 单元测试

至少覆盖：

- 输入标准化与校验；
- 关联度区间到直白等级文案的映射；
- 精确命中规则；
- 非答案分数封顶 99.900%；
- 余弦相似度和校准函数；
- 重复猜测判断；
- 题库结构校验；
- 提示顺序；
- 本地战绩统计。

### 2. API / 集成测试

至少覆盖：

- 创建游戏不会泄露答案；
- 正常猜测返回正确结构；
- 重复猜测不增加次数；
- 无效输入被拒绝；
- 三次提示按顺序返回；
- 第四次提示被拒绝；
- 猜中后不能继续猜；
- 放弃后不能继续猜；
- 不存在或无效的游戏局被安全处理；
- 评分服务失败时返回可恢复错误，不破坏本局状态。

### 3. 端到端测试

使用稳定的浏览器自动化测试覆盖完整用户路径：

1. 打开首页并开始一局；
2. 输入低关联词并看到反馈；
3. 输入高关联词并看到最佳记录更新；
4. 切换历史排序；
5. 重复输入不增加猜测次数；
6. 使用提示并在刷新后恢复；
7. 刷新页面后恢复当前局和猜测历史；
8. 输入正确答案并看到结算；
9. 开始新游戏；
10. 点击“查看答案”，确认结束本局并看到完整结果。

至少在一个手机视口和一个桌面视口验证主要流程。检查控制台没有未处理异常，页面没有明显横向溢出，主要按钮在手机端可操作。

### 4. 构建与静态检查

- 生产构建必须成功；
- TypeScript 类型检查必须成功；
- 项目已有 lint 时必须通过；
- 测试不能依赖执行顺序；
- 不允许以删除、跳过或弱化测试来掩盖失败。

## 八、验收门槛

只有同时满足以下条件，才可以宣布任务完成：

1. 应用可以按照 README 从干净环境启动；
2. 不配置真实模型时，开发者可明确选择测试模式运行完整流程；
3. 配置真实模型后，生产路径不会使用测试评分；
4. 核心游戏流程端到端可玩；
5. 页面刷新可以恢复未结束游戏；
6. 答案未出现在前端代码、初始化响应、本地存储或普通猜测响应中；
7. 精确答案为 100.000%，其他输入最高为 99.900%；
8. 重复猜测、提示、放弃和已结束游戏的边界行为正确；
9. 单元、集成、端到端测试和生产构建全部通过；
10. 手机和桌面布局完成基本验收；
11. README、环境变量示例和验收报告完整；
12. 没有把未验证事项描述成已完成。

任何未通过项都必须继续修复并重新验证。如果确实被外部密钥或账号阻塞，将其标为“外部阻塞”，同时完成其余所有可验证工作。

## 九、最终交付物

完成后仓库中至少应包含：

- 可运行的应用源代码；
- 服务端题库与题库校验；
- 语义评分适配器及确定性测试适配器；
- 单元、API/集成和端到端测试；
- `.env.example`；
- `README.md`，说明安装、配置、开发、测试和构建方式；
- `ACCEPTANCE_REPORT.md`，逐项记录验收结果、执行命令、测试数量、已知限制和外部阻塞；
- 必要的架构说明或关键决策记录。

## 十、最终回复格式

最终回复必须简洁、基于实际结果，并包含：

1. 已完成的产品能力；
2. 采用的技术方案和语义模型方案；
3. 实际执行的测试与结果；
4. 验收是否通过；
5. 仍存在的限制或需要用户提供的配置；
6. 关键文件的位置；
7. 本地启动方式。

不要只说“已经完成”或“测试通过”。必须提供可以核对的文件、测试结果和未完成事项。如果真实语义模型没有使用有效凭据完成端到端验证，应明确写出“真实语义评分尚未验证”，不能用测试适配器的通过结果替代。
