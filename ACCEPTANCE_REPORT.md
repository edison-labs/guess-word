# GuessWord 工程 MVP 验收报告

验收日期：2026-09-05
验收结论：**工程 MVP 准出（PASS）；适合单 ECS 朋友内测。**

2026-09-05 增量版本新增游客身份、用户名口令与一次性恢复码、可选阿里云短信登录、游客战绩合并、用户中心、每日挑战榜和好友同题榜。榜单只比较相同题目，每位登录用户只取首次作答；账号会话使用 HttpOnly Cookie，口令使用带独立盐的 PBKDF2-SHA256 哈希，恢复码、手机号与验证码不明文落库。SQLite 旧库升级、账号切换隔离、登录限流、恢复码轮换和验证码一次性消费均已纳入自动化回归。

本报告只记录实际完成和验证的结果。Cloudflare 与 DeepSeek 真实适配器均已实现；DeepSeek 已用本地服务端凭据完成受控中文语义标定，自动化和浏览器回归另使用 mock 供应商与显式选择、仅允许在 development/test 环境运行的确定性适配器。

## 三角色评审记录

| 角色 | 首轮产出 | 结论 |
|---|---|---|
| 产品 Agent | 评审 `PRODUCT_DESIGN.md` 与 `AI_BUILD_PROMPT.md`，冻结 P0 范围、统计口径和分层准出门槛 | 有条件通过，先交付工程 MVP |
| 开发 Agent | 评审服务端状态机、D1 数据结构、恢复令牌、评分器边界和答案隔离方案 | 架构可实施，真实与测试评分路径必须强隔离 |
| 测试 Agent | 设计输入、API、恢复、提示、终局、安全、响应式和无障碍测试矩阵 | P0 必须由单元、集成、E2E、构建和泄漏扫描共同覆盖 |

冻结后的产品口径见 `REQUIREMENTS_REVIEW.md`；主 Agent 依据三份评审结果完成实现、缺陷修复和最终回归。

## 已交付能力

- 游客可直接游玩的八分类选择、分类内随机开局、不可枚举游戏 ID 与独立恢复令牌；刷新可恢复活动局、猜测历史和提示状态。
- 1～10 个连续汉字的 NFKC 标准化与前后端校验；重复输入不会调用评分器或增加次数。
- 0.000%～100.000% 数值、直白关联等级和进度条反馈；精确答案为 100.000%，所有非答案统一封顶 99.900%。
- 关联度/时间双排序、当前最佳、有效猜测数、两级渐进提示、“查看答案”二次确认，以及不重复展示状态提示和概况的单一结算区。
- 仅保存无答案的本地战绩；总局数、胜局数、最佳胜局次数和最近 20 局均按冻结口径计算。
- 200 道服务端题目，八分类各 25 道，并自动校验唯一 ID、字段完整性、答案长度、分类分布和提示词安全性。
- 用户名口令注册登录、一次性恢复码找回、可选手机验证码登录、游客战绩合并、昵称、跨设备用户战绩、连续挑战统计、每日挑战榜和好友同题榜；短信生产适配器使用阿里云官方 V2 SDK。
- Cloudflare D1 与阿里云 Node/SQLite WAL 双数据路径、评分前原子占位去重、六个同源 API、统一错误结构、健康检查、Origin/令牌/请求体/本地限流校验和安全响应头。
- Cloudflare Workers AI `@cf/baai/bge-m3` embeddings 适配器、DeepSeek V4 Flash JSON judge 适配器，以及仅限 development/test 的确定性适配器；生产配置缺失或选择测试适配器时明确失败。
- 借鉴高密度联想猜词榜的交互模式：375×812 手机端和 1440×900 桌面端窄单列、输入优先、关联度/时间榜单、键盘操作、可见焦点、文本化状态、减少动画支持和严重/致命无障碍缺陷扫描。

## 实际执行证据

所有命令均在最终 `package-lock.json` 对应的依赖树上执行。

| 检查 | 命令 | 实际结果 |
|---|---|---|
| 依赖锁定 | `npm install --package-lock-only --offline` | 通过；`package.json` 与最终 lockfile 一致 |
| 类型检查 | `npm run typecheck` | 通过，0 错误 |
| Lint | `npm run lint` | 通过，0 错误 |
| 单元与集成测试 | `npm test` | 10 个文件、109/109 通过；包含账号隔离、验证码一次性消费、榜单规则和旧 SQLite 升级 |
| E2E | `npm run test:e2e` | 桌面与手机各覆盖完整流程、同题分享、每日挑战、短信登录、游客战绩合并和好友榜；12/12 通过，既有 axe serious/critical 检查为 0 |
| 生产构建 | `npm run build`、`npm run build:aliyun` | Sites 与阿里云 standalone 均通过；首页和 17 个动态 API Route Handlers 成功产出 |
| 客户端泄漏扫描 | `npm run check:leaks` | 扫描 800 个私有题库值；0 泄漏。登录 UI 必需的公共词“手机”有显式白名单 |
| 构建密钥扫描 | `npm run check:secrets`、`npm run check:aliyun-secrets` | DeepSeek、AUTH_SECRET 和阿里云短信哨兵均未进入客户端或公开构建产物 |
| DeepSeek 真实标定 | 固定答案“企鹅”，提交 4 个非答案词 | `scoringMode=semantic`；律师 0.000%、动物 27.252%、鸟类 47.576%、南极 52.239%；后三项为真实非零小数尾数，排序方向合理 |
| 数据库迁移 | `npm run db:generate` | 通过；9 张表、迁移 `0005` 与当前 schema 一致，旧库无损升级集成测试通过 |
| 依赖安全 | `npm audit` | 0 vulnerabilities |
| 浏览器目视验收 | 本地 Chrome | 页面加载和真实交互通过；提交“南极”显示 99.900%/“非常接近”，输入焦点保持，控制台 0 错误 |

覆盖率未设置为伪造的全局门槛；剩余低覆盖主要集中在 D1 运行时实现。Cloudflare 与 DeepSeek 适配器已用 mock HTTP 覆盖请求、缓存、加权、鉴权/限流分类和非法响应；DeepSeek 另完成真实 API 标定，关键业务规则、状态机和 API 行为由单元/集成/E2E 交叉覆盖。

## 十二项验收门槛

| # | 门槛 | 状态 | 证据 |
|---:|---|---|---|
| 1 | 按 README 从干净环境启动 | PASS | `npm ci`、本地 dev server、HTTP 200 和 Chrome 加载均通过 |
| 2 | 无真实模型时可显式选择测试模式 | PASS | `.dev.vars.example` 明确配置 `SEMANTIC_PROVIDER=deterministic` |
| 3 | 生产路径不使用测试评分 | PASS | production + deterministic 或配置缺失会明确失败，测试覆盖配置边界 |
| 4 | 核心流程端到端可玩 | PASS | 桌面/手机完整 E2E 与浏览器目视验收通过 |
| 5 | 刷新恢复未结束游戏 | PASS | E2E 验证猜测与提示在刷新后恢复 |
| 6 | 活动局及客户端不泄露答案 | PASS | API 白名单、localStorage 断言、800 个私有题库字段扫描和哨兵密钥构建扫描通过 |
| 7 | 精确 100%，非答案最高 99.9% | PASS | 领域规则、服务集成和 E2E 共同验证 |
| 8 | 重复、提示、放弃、终局边界正确 | PASS | 单元/API/E2E 均覆盖 |
| 9 | 单元、集成、E2E、构建全部通过 | PASS | 109/109 Vitest、最终源码 E2E 12/12、双平台构建通过 |
| 10 | 手机与桌面完成基本验收 | PASS | 375×812、1440×900；无明显横向溢出，主要操作可用 |
| 11 | README、环境示例和验收报告完整 | PASS | 三类交付物均已提供 |
| 12 | 不把未验证事项描述为完成 | PASS | Cloudflare、真人题库和部署限制在下节明确列出 |

## 已修复缺陷与安全收尾

- 修正移动端 E2E 浏览器配置、无 ffmpeg 环境下的视频依赖、刷新时误清 localStorage、重复猜测产生 409 控制台噪声，以及 D1 并发序号写入。
- 修复构建时环境 Token 被 Cloudflare config 当作普通 vars 序列化的 P0：生产构建不再注入 process env，Token 只允许来自本地 `.dev.vars` 或平台加密 secret binding，并新增哨兵密钥构建门禁。
- 新增 D1 `guess_claims` 评分前原子占位、claim token 所有权和超时接管；同一标准化词的并发请求现在只调用一次评分器，失败仍可重试。
- 修复运行时初始化 rejected Promise 永久缓存和错误误分类；瞬时 D1 故障可在下一请求自愈，配置错误与可重试依赖故障分别返回。
- 将“放弃本局”入口改为更易发现的“查看答案”，确认后展示答案、次数、用时、提示、最佳联想和完整猜词记录；弹窗保留焦点循环和关闭后焦点归还。
- 修复确定性测试评分器对全部未登记词统一返回 5.5%：未知词现在使用稳定的低分散列扰动，类别和题目线索仍按预设层次反馈；页面明确显示“测试评分”，避免把该结果误认为真实语义。
- 低关联提示统一为直白的“几乎无关/关联较弱”，并保持低分蓝灰色在浅色背景上的可读对比度；桌面和手机 axe 复验的 serious/critical 违规均为 0。
- 将界面重构为移动端优先的高密度本局猜词榜和八分类开局页；结算时隐藏进行中提示、概况和评分说明，只保留一次答案与统计。关系等级和说明文案按真实关系逐步收敛，评分全链路使用整数千分位百分比。
- 强化真实 AI 评分器：首次猜词与答案 embeddings 合并为一次批量请求，答案向量可复用；真实请求超时由 2.5 秒调整为 8 秒并同步延长猜词占位；坏 Token、账户或模型配置与 429/5xx/超时现在按不可重试/可重试分别处理。
- 测试和 AI 模式的规则、首屏说明及输入提示分别渲染；只有真实模式会显示“AI 语义评分”，测试模式不再声称其数值代表真实词义远近。
- 新增 `deepseek-judge`：使用 DeepSeek V4 Flash 非思考模式和 JSON 输出，以三位小数分别判断词义接近、场景关联和目标指向性，再按固定权重计算并以整数千分位百分比保存；精度来自模型判断而非补零或随机尾数，精确答案不调用模型，非答案封顶 99.900%，坏密钥与临时故障分别处理。
- DeepSeek API Key 只允许来自本地 `.dev.vars` 或平台加密 secret；生产构建的双供应商哨兵扫描、源码通用 `sk-` 模式扫描和客户端私有题库扫描均为 0 命中。
- 升级 Next.js、React、Vinext、Vite、Cloudflare Vite Plugin、Wrangler 和 RSC 相关依赖到相互兼容的安全补丁版本。
- 最新稳定 `drizzle-kit` 仍传递依赖旧 `esbuild`；项目使用 Drizzle 自身已采用的安全版 `0.25.12` 做精确 override，并以迁移生成、类型检查、测试和构建验证兼容性。最终完整 `npm audit` 为 0 漏洞。
- ESLint 10 虽满足 Next 顶层 peer 声明，但 Next 16.3.3 内置的 React/import/a11y 插件仍只声明支持至 ESLint 9，因此保留插件链有效的最新 9.x；Lint 实际通过。

## 限制与外部阻塞

1. **DeepSeek 已完成本地真实验证，Cloudflare 尚未验证。** 固定题目 4 词标定确认 DeepSeek 返回非统一且符合冷到热方向的分数；聊天中曾暴露的旧 Key 仍应在供应商控制台确认已撤销。Cloudflare embeddings 适配器仍只有 mock HTTP 验证。
2. 当前为 200 道结构完整题目，八类各 25 道；数量门槛已满足，但仍需持续做逐题真人试玩与真实模型标定。
3. 阿里云 standalone 已完成本地生产进程、健康检查、SQLite 写入和重启恢复验收，但尚未连接目标 ECS 完成公网部署。
4. 当前限流为手机号持久限流加单进程客户端/总量保护，仅满足小范围试玩；公开推广前必须增加网关分布式限流、验证码风控和监控告警。
5. 当前包含短信登录、跨设备战绩、每日挑战、同题分享和公平榜单；不包含微信登录、好友关系、支付、广告、实时多人或运营后台。

因此，当前版本可以作为**本地可运行、DeepSeek 真实评分和自动化验收均通过的工程 MVP**用于单 ECS 朋友内测；短信登录只有在阿里云签名、模板和凭据配置完成后才可用。公开推广前仍需完成真人题库标定、HTTPS、RDS/分布式限流与账号注销等能力。
